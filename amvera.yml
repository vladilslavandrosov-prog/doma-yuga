meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: npm install --include=dev && npm run build
run:
  persistenceMount: /data
  scriptName: null
  scriptArguments: null
  nodeArguments: null
  command: npm run db:push && node dist/index.cjs
  containerPort: 5000
  servicePort: 80
